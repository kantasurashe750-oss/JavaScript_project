let number = Number(document.querySelector("#number").value);

if (number % 2 === 0) {
    console.log("Zero:", number)
} else if (number > 0) {

    if (/* even? */) {
        // Positive Even
    } else {
        // Positive Odd
    }

} else {

    if (/* even? */) {
        // Negative Even
    } else {
        // Negative Odd
    }
}